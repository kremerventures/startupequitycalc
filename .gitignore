# Local Netlify folder (machine-specific site linkage)
.netlify/

# Claude Code machine-local settings (permission grants, etc.)
.claude/settings.local.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Node (only if a build step is ever added)
node_modules/
